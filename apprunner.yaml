version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Docker build definido no Dockerfile"
run:
  runtime-version: latest
  command: nginx -g 'daemon off;'
  network:
    port: 8080
    env: PORT
